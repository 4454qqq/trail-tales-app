import { Text, View } from "react-native";

export default function LogPublic() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>游记发布</Text>
    </View>
  );
}
